apiVersion: apps/v1
kind: Deployment
metadata:
  name: devpulse-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devpulse-redis
  template:
    metadata:
      labels:
        app: devpulse-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: devpulse-redis
spec:
  selector:
    app: devpulse-redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP
